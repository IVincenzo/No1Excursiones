services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: no1_excursiones
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - no1-postgres:/var/lib/postgresql/data

volumes:
  no1-postgres:
